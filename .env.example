TASKS_BASE_URL=http://localhost:3000
TASKS_API_TOKEN=tdc_replace_me
# Remote HTTP mode only:
MCP_PORT=3001
MCP_HOST=127.0.0.1
MCP_ALLOWED_ORIGINS=http://localhost:3000
# Preferred connect-by-URL mode (configure both):
# MCP_PUBLIC_URL=https://mcp.example.com/mcp
# MCP_OAUTH_PASSWORD=generate-a-separate-16-byte-or-longer-password
# MCP_OAUTH_STORE_PATH=./data/oauth-store.json
# Static fallback. One auth mode is required if MCP_HOST is not loopback.
# MCP_BEARER_TOKEN=generate-a-long-random-value
